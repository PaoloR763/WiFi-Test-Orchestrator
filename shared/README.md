# Shared contracts

`shared/contracts/` is the sole normative source for protocol payloads,
operations, catalogues and golden examples. Runtime copies are generated and
must match this directory byte-for-byte through `scripts/build_openapi.py` and
`scripts/validate_contracts.py`.
